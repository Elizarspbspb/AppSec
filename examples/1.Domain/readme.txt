Domain / Path cookies - атрибуты cookie.

Например: 
Set-Cookie: session=abc; Domain=example.com
и:
Set-Cookie: session=abc; Path=/admin

Определяют, для каких хостов и путей cookie применяется. Это важно при изучении атак на cookies и subdomain security.